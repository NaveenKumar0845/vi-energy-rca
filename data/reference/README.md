# Reference data

The bundled `dispute_reasons.csv` is a lightweight taxonomy used by the synthetic demo.

If an internal Excel taxonomy is used locally, place it here as `dispute_reasons.xlsx`; `.gitignore` prevents Excel reference workbooks from being committed.

The model always constrains EB records to EB reasons and DG records to DG reasons.
