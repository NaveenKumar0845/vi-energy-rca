# Optional network KPI data
If real network data becomes available, upload `network_kpis.xlsx` here. Supported fields can include RSRP, RSRQ, SINR, DL/UL throughput, PRB utilization, availability and alarm count.

Without this file the application remains a billing/dispute RCA solution and does not claim 5G/network root causes.
